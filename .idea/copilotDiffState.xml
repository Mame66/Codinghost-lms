<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="/homeworks (snapshot).js">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="/homeworks (snapshot).js" />
              <option name="originalContent" value="const express = require('express');&#10;const router = express.Router();&#10;const { protect, allowRoles } = require('../middleware/authMiddleware');&#10;const { PrismaClient } = require('@prisma/client');&#10;&#10;const prisma = new PrismaClient();&#10;&#10;// GET tous les devoirs (enseignant)&#10;router.get('/', protect, async (req, res) =&gt; {&#10;    try {&#10;        const homeworks = await prisma.homework.findMany({&#10;            include: {&#10;                task: { select: { titre: true, type: true } },&#10;                student: {&#10;                    include: {&#10;                        user: { select: { nom: true, prenom: true } }&#10;                    }&#10;                },&#10;                files: true,&#10;            },&#10;            orderBy: { createdAt: 'desc' }&#10;        });&#10;        res.json(homeworks);&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// GET devoirs d'un étudiant&#10;router.get('/student/:studentId', protect, async (req, res) =&gt; {&#10;    try {&#10;        const homeworks = await prisma.homework.findMany({&#10;            where: { studentId: parseInt(req.params.studentId) },&#10;            include: {&#10;                task: { select: { titre: true, type: true } },&#10;                files: true,&#10;            },&#10;            orderBy: { createdAt: 'desc' }&#10;        });&#10;        res.json(homeworks);&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// POST soumettre un devoir&#10;router.post('/', protect, async (req, res) =&gt; {&#10;    const { taskId, studentId, contenu, lienRendu, qcmAnswers } = req.body;&#10;    try {&#10;        const existing = await prisma.homework.findFirst({&#10;            where: {&#10;                taskId: parseInt(taskId),&#10;                studentId: parseInt(studentId),&#10;            }&#10;        });&#10;        if (existing) {&#10;            // Mettre à jour si déjà soumis&#10;            const updated = await prisma.homework.update({&#10;                where: { id: existing.id },&#10;                data: {&#10;                    contenu,&#10;                    lienRendu,&#10;                    qcmAnswers,&#10;                    statut: 'RENDU',&#10;                }&#10;            });&#10;            return res.json({ message: 'Devoir mis à jour', homework: updated });&#10;        }&#10;&#10;        const homework = await prisma.homework.create({&#10;            data: {&#10;                taskId: parseInt(taskId),&#10;                studentId: parseInt(studentId),&#10;                contenu,&#10;                lienRendu,&#10;                qcmAnswers,&#10;                statut: 'RENDU',&#10;            }&#10;        });&#10;&#10;        // Notification à l'enseignant&#10;        const task = await prisma.task.findUnique({&#10;            where: { id: parseInt(taskId) },&#10;            include: {&#10;                chapter: {&#10;                    include: {&#10;                        course: {&#10;                            include: {&#10;                                group: {&#10;                                    include: { teacher: true }&#10;                                }&#10;                            }&#10;                        }&#10;                    }&#10;                }&#10;            }&#10;        });&#10;&#10;        if (task?.chapter?.course?.group?.teacher) {&#10;            const student = await prisma.student.findUnique({&#10;                where: { id: parseInt(studentId) },&#10;                include: { user: true }&#10;            });&#10;            await prisma.notification.create({&#10;                data: {&#10;                    userId: task.chapter.course.group.teacher.id,&#10;                    titre: ' Nouveau devoir reçu',&#10;                    message: `${student.user.prenom} ${student.user.nom} a rendu : ${task.titre}`,&#10;                    type: 'DEVOIR',&#10;                }&#10;            });&#10;        }&#10;&#10;        res.status(201).json({ message: 'Devoir soumis', homework });&#10;    } catch (err) {&#10;        console.error(err);&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// PUT corriger un devoir&#10;router.put('/:id', protect, allowRoles('ADMIN', 'TEACHER'), async (req, res) =&gt; {&#10;    const { note, commentaire } = req.body;&#10;    try {&#10;        const homework = await prisma.homework.update({&#10;            where: { id: parseInt(req.params.id) },&#10;            data: {&#10;                note: note ? parseFloat(note) : null,&#10;                commentaire,&#10;                statut: 'CORRIGE',&#10;            },&#10;            include: {&#10;                student: { include: { user: true } },&#10;                task: true,&#10;            }&#10;        });&#10;&#10;        // Notification étudiant&#10;        await prisma.notification.create({&#10;            data: {&#10;                userId: homework.student.userId,&#10;                titre: ' Nouvelle note reçue',&#10;                message: `Vous avez reçu ${note}/20 pour : ${homework.task.titre}`,&#10;                type: 'NOTE',&#10;            }&#10;        });&#10;&#10;        res.json(homework);&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// DELETE supprimer un devoir&#10;router.delete('/:id', protect, allowRoles('ADMIN'), async (req, res) =&gt; {&#10;    try {&#10;        await prisma.homeworkFile.deleteMany({ where: { homeworkId: parseInt(req.params.id) } });&#10;        await prisma.homework.delete({ where: { id: parseInt(req.params.id) } });&#10;        res.json({ message: 'Devoir supprimé' });&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;module.exports = router;" />
              <option name="updatedContent" value="const express = require('express');&#10;const router = express.Router();&#10;const { protect, allowRoles } = require('../middleware/authMiddleware');&#10;const { PrismaClient } = require('@prisma/client');&#10;&#10;const prisma = new PrismaClient();&#10;&#10;// Helper function to create notification&#10;const createNotification = async (userId, titre, message, type) =&gt; {&#10;    await prisma.notification.create({&#10;        data: { userId, titre, message, type }&#10;    });&#10;};&#10;&#10;// Validation helpers&#10;const validateNumber = (value) =&gt; !isNaN(value) &amp;&amp; Number.isInteger(Number(value));&#10;const validateNote = (note) =&gt; note === null || (typeof note === 'number' &amp;&amp; note &gt;= 0 &amp;&amp; note &lt;= 20);&#10;&#10;// GET tous les devoirs (enseignant)&#10;router.get('/', protect, allowRoles('ADMIN', 'TEACHER'), async (req, res) =&gt; {&#10;    try {&#10;        const homeworks = await prisma.homework.findMany({&#10;            include: {&#10;                task: { select: { titre: true, type: true } },&#10;                student: {&#10;                    include: {&#10;                        user: { select: { nom: true, prenom: true } }&#10;                    }&#10;                },&#10;                files: true,&#10;            },&#10;            orderBy: { createdAt: 'desc' }&#10;        });&#10;        res.json(homeworks);&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// GET devoirs d'un étudiant&#10;router.get('/student/:studentId', protect, async (req, res) =&gt; {&#10;    const { studentId } = req.params;&#10;    if (!validateNumber(studentId)) {&#10;        return res.status(400).json({ message: 'ID étudiant invalide' });&#10;    }&#10;&#10;    try {&#10;        // Authorization check&#10;        if (req.user.role === 'STUDENT') {&#10;            const student = await prisma.student.findUnique({ where: { userId: req.user.id } });&#10;            if (!student || student.id !== parseInt(studentId)) {&#10;                return res.status(403).json({ message: 'Accès refusé' });&#10;            }&#10;        } else if (!['ADMIN', 'TEACHER'].includes(req.user.role)) {&#10;            return res.status(403).json({ message: 'Accès refusé' });&#10;        }&#10;&#10;        const homeworks = await prisma.homework.findMany({&#10;            where: { studentId: parseInt(studentId) },&#10;            include: {&#10;                task: { select: { titre: true, type: true } },&#10;                files: true,&#10;            },&#10;            orderBy: { createdAt: 'desc' }&#10;        });&#10;        res.json(homeworks);&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// POST soumettre un devoir&#10;router.post('/', protect, async (req, res) =&gt; {&#10;    const { taskId, studentId, contenu, lienRendu, qcmAnswers } = req.body;&#10;&#10;    if (!validateNumber(taskId) || !validateNumber(studentId)) {&#10;        return res.status(400).json({ message: 'IDs invalides' });&#10;    }&#10;&#10;    try {&#10;        // Authorization check&#10;        if (req.user.role === 'STUDENT') {&#10;            const student = await prisma.student.findUnique({ where: { userId: req.user.id } });&#10;            if (!student || student.id !== parseInt(studentId)) {&#10;                return res.status(403).json({ message: 'Accès refusé' });&#10;            }&#10;        } else {&#10;            return res.status(403).json({ message: 'Seuls les étudiants peuvent soumettre des devoirs' });&#10;        }&#10;&#10;        const existing = await prisma.homework.findFirst({&#10;            where: {&#10;                taskId: parseInt(taskId),&#10;                studentId: parseInt(studentId),&#10;            }&#10;        });&#10;        if (existing) {&#10;            // Mettre à jour si déjà soumis&#10;            const updated = await prisma.homework.update({&#10;                where: { id: existing.id },&#10;                data: {&#10;                    contenu,&#10;                    lienRendu,&#10;                    qcmAnswers,&#10;                    statut: 'RENDU',&#10;                }&#10;            });&#10;            return res.json({ message: 'Devoir mis à jour', homework: updated });&#10;        }&#10;&#10;        const homework = await prisma.homework.create({&#10;            data: {&#10;                taskId: parseInt(taskId),&#10;                studentId: parseInt(studentId),&#10;                contenu,&#10;                lienRendu,&#10;                qcmAnswers,&#10;                statut: 'RENDU',&#10;            }&#10;        });&#10;&#10;        // Notification à l'enseignant&#10;        const task = await prisma.task.findUnique({&#10;            where: { id: parseInt(taskId) },&#10;            include: {&#10;                chapter: {&#10;                    include: {&#10;                        course: {&#10;                            include: {&#10;                                group: {&#10;                                    include: { teacher: true }&#10;                                }&#10;                            }&#10;                        }&#10;                    }&#10;                }&#10;            }&#10;        });&#10;&#10;        if (task?.chapter?.course?.group?.teacher) {&#10;            const student = await prisma.student.findUnique({&#10;                where: { id: parseInt(studentId) },&#10;                include: { user: true }&#10;            });&#10;            await createNotification(&#10;                task.chapter.course.group.teacher.id,&#10;                ' Nouveau devoir reçu',&#10;                `${student.user.prenom} ${student.user.nom} a rendu : ${task.titre}`,&#10;                'DEVOIR'&#10;            );&#10;        }&#10;&#10;        res.status(201).json({ message: 'Devoir soumis', homework });&#10;    } catch (err) {&#10;        console.error(err);&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// PUT corriger un devoir&#10;router.put('/:id', protect, allowRoles('ADMIN', 'TEACHER'), async (req, res) =&gt; {&#10;    const { id } = req.params;&#10;    const { note, commentaire } = req.body;&#10;&#10;    if (!validateNumber(id)) {&#10;        return res.status(400).json({ message: 'ID invalide' });&#10;    }&#10;&#10;    if (!validateNote(note)) {&#10;        return res.status(400).json({ message: 'Note invalide (0-20)' });&#10;    }&#10;&#10;    try {&#10;        const homework = await prisma.homework.update({&#10;            where: { id: parseInt(id) },&#10;            data: {&#10;                note: note ? parseFloat(note) : null,&#10;                commentaire,&#10;                statut: 'CORRIGE',&#10;            },&#10;            include: {&#10;                student: { include: { user: true } },&#10;                task: true,&#10;            }&#10;        });&#10;&#10;        // Notification étudiant&#10;        await createNotification(&#10;            homework.student.userId,&#10;            ' Nouvelle note reçue',&#10;            `Vous avez reçu ${note}/20 pour : ${homework.task.titre}`,&#10;            'NOTE'&#10;        );&#10;&#10;        res.json(homework);&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;// DELETE supprimer un devoir&#10;router.delete('/:id', protect, allowRoles('ADMIN'), async (req, res) =&gt; {&#10;    const { id } = req.params;&#10;&#10;    if (!validateNumber(id)) {&#10;        return res.status(400).json({ message: 'ID invalide' });&#10;    }&#10;&#10;    try {&#10;        await prisma.homeworkFile.deleteMany({ where: { homeworkId: parseInt(id) } });&#10;        await prisma.homework.delete({ where: { id: parseInt(id) } });&#10;        res.json({ message: 'Devoir supprimé' });&#10;    } catch (err) {&#10;        res.status(500).json({ message: 'Erreur serveur', error: err.message });&#10;    }&#10;});&#10;&#10;module.exports = router;" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>